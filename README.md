# Saigon VN — new website

Новый коммерческий сайт ресторана Saigon VN в Королёве.

## Концепция

Сайт имеет две полноценные визуальные версии — тёмную и светлую. Обе сохраняют одну структуру, контент и CTA, но используют разные поверхности, контраст и настроение. Переключатель темы доступен в шапке на desktop и внутри мобильного меню. Выбор сохраняется в `localStorage`; при первом визите учитывается системная тема устройства.

Меню — интерактивный блок: сверху показываются рекомендуемые блюда, ниже по кнопке раскрывается полное меню с поиском, категориями и карточкой выбранного блюда справа. Для позиций, у которых есть изображения в `assets/menu/`, карточка показывает фото; для остальных используется нейтральный placeholder до добавления изображения.

## Технологии

- semantic HTML5
- responsive CSS без UI-фреймворков
- минимальный vanilla JavaScript
- отдельная светлая и тёмная тема
- SVG-анимация пара над главным блюдом с turbulence/displacement/blur
- автоматическое отключение анимации при `prefers-reduced-motion`
- Restaurant schema.org markup
- mobile sticky action bar
- responsive breakpoints для 375 / 430 / 768 / 1024 / 1440 px
- отдельная 404-страница и robots.txt

Сборщик, npm-зависимости и backend для запуска сайта не нужны.

## Резервная версия и откат

Перед переработкой меню 12 августа 2026 года было зафиксировано стабильное состояние сайта в отдельной ветке:

```text
backup/pre-menu-redesign-2026-08-12
```

Эта ветка является точкой отката к версии сайта до текущей переработки меню. Не удалять её без отдельного решения.

### Посмотреть резервную версию локально

```bash
git fetch origin
git switch backup/pre-menu-redesign-2026-08-12
```

Чтобы снова вернуться к актуальной разработке:

```bash
git switch main
git pull origin main
```

### Полностью вернуть `main` к резервной версии

Это операция с потерей всех коммитов в `main`, сделанных после точки резервного копирования, поэтому перед выполнением рекомендуется сначала создать дополнительную страховочную ветку:

```bash
git switch main
git pull origin main
git branch backup/before-rollback
git push origin backup/before-rollback
```

После этого откатить `main`:

```bash
git reset --hard origin/backup/pre-menu-redesign-2026-08-12
git push --force-with-lease origin main
```

После обновления `main` GitHub Pages опубликует состояние сайта из восстановленной версии.

## Установка локального репозитория

Понадобится Git. Откройте Terminal / PowerShell и выполните:

```bash
git clone https://github.com/Benzilya/saigon-new.git
cd saigon-new
```

Если репозиторий уже клонирован, повторно клонировать его не нужно.

## Запуск локально

Рекомендуется запускать сайт через простой HTTP-сервер, а не двойным кликом по `index.html`: так поведение будет ближе к реальному хостингу.

### Вариант 1 — Python

```bash
python -m http.server 8080
```

На Windows также может использоваться:

```bash
py -m http.server 8080
```

После запуска открыть `http://localhost:8080`.

### Как выбрать другой порт

Число в конце команды — это порт. Например:

```bash
python -m http.server 3000
python -m http.server 5500
python -m http.server 9000
```

Тогда адреса будут соответственно `http://localhost:3000`, `http://localhost:5500` или `http://localhost:9000`.

Если терминал пишет `Address already in use`, выберите другой порт, например 8081 или 9000. Чтобы остановить локальный сервер, нажмите `Ctrl + C`.

### Вариант 2 — Node.js

```bash
npx serve . -l 8080
```

Открыть `http://localhost:8080`.

### Вариант 3 — VS Code

Можно установить расширение Live Server, открыть папку проекта и нажать `Go Live`. Порт отображается в адресной строке браузера и обычно равен 5500.

## Как получить последние обновления

```bash
git switch main
git pull origin main
```

Проверить состояние:

```bash
git status
```

Если есть локальные изменения и `pull` не выполняется:

```bash
git stash
git pull origin main
git stash pop
```

После `stash pop` проверьте возможные конфликты.

## Как сохранить свои изменения

```bash
git status
git add .
git commit -m "Describe changes"
git push origin main
```

Для серьёзных изменений безопаснее отдельная ветка:

```bash
git switch -c feature/my-change
git add .
git commit -m "Describe changes"
git push -u origin feature/my-change
```

## Структура сайта

Логическая структура страницы:

```text
Saigon VN
├── Header
│   ├── Логотип
│   ├── Основная навигация
│   │   ├── Меню
│   │   ├── О ресторане
│   │   ├── Отзывы
│   │   └── Контакты
│   ├── Переключатель темы
│   ├── Телефон
│   └── CTA «Забронировать»
├── Hero
│   ├── Основной оффер
│   ├── CTA «Смотреть меню и цены»
│   ├── CTA «Забронировать стол»
│   └── Рейтинг Яндекс Карт
├── Блок преимуществ
├── Меню
│   ├── Карусель рекомендуемых блюд
│   ├── Кнопка раскрытия полного меню
│   └── Полное меню
│       ├── Поиск
│       ├── Категории
│       ├── Список блюд
│       └── Карточка выбранного блюда
│           ├── Фото / placeholder
│           ├── Название
│           ├── Цена
│           ├── Вес
│           ├── Описание
│           └── Состав
├── О ресторане / атмосфера
├── Отзывы
├── Контакты и маршрут
├── Бронирование
├── Footer
└── Mobile sticky actions
    ├── Меню
    ├── Позвонить
    └── Маршрут
```

## Структура репозитория

```text
saigon-new/
├── index.html                 # основная HTML-структура, SEO, Schema.org
├── app.js                     # темы, мобильная навигация и общая интерактивность
├── menu.js                    # данные меню, карусель, поиск, фильтры и detail-карточка
├── styles.css                 # базовая визуальная система
├── theme.css                  # светлая/тёмная темы
├── steam.css                  # визуальный эффект пара
├── menu.css                   # основная стилизация интерактивного меню
├── responsive.css             # общие адаптивные правила
├── header-qa.css              # корректировки шапки
├── desktop-foundation.css     # desktop foundation overrides
├── desktop-hero-header.css    # desktop header/hero overrides
├── desktop-menu.css           # дополнительные desktop-стили меню
├── desktop-sections.css       # desktop-стили остальных секций
├── assets/
│   └── menu/
│       ├── pho-bo.webp
│       ├── tom-yam.webp
│       ├── bun-cha.webp
│       ├── nem-haisan.webp
│       └── com-tom-sot.webp
├── 404.html                   # страница 404
├── robots.txt                 # правила для поисковых роботов
└── README.md                  # эта инструкция
```

При добавлении нового изображения блюда рекомендуется использовать путь:

```text
assets/menu/<slug-блюда>.webp
```

и затем связать изображение с блюдом в `menu.js`.

## Темы

Тема задаётся атрибутом `data-theme="dark"` или `data-theme="light"` у элемента `<html>`. Пользовательский выбор сохраняется под ключом `saigon-theme`.

Чтобы сбросить сохранённый выбор в DevTools:

```js
localStorage.removeItem('saigon-theme')
location.reload()
```

После этого сайт снова возьмёт системную тему устройства.

## Публикация

Сайт статический, поэтому его можно публиковать на GitHub Pages, Netlify, Cloudflare Pages, Vercel или обычном веб-хостинге. Build command не требуется; публикуется корень репозитория.

Для GitHub Pages: `Settings` → `Pages` → `Build and deployment` → `Deploy from a branch` → `main` → `/ (root)` → `Save`.

## Что менять чаще всего

- контент и SEO: `index.html`
- меню, цены, составы, изображения и интерактивность меню: `menu.js`
- внешний вид меню: `menu.css` и `desktop-menu.css`
- основная визуальная система: `styles.css`
- светлая/тёмная темы: `theme.css`
- адаптивность: `responsive.css` и desktop override-файлы
- эффект пара: `steam.css` и логика в `app.js`

## Источники фактических данных

Контакты, адрес, рейтинг, отзывы и меню должны сверяться с актуальными источниками ресторана, прежде всего карточкой Saigon VN на Яндекс Картах.

Иллюстрации блюд, созданные специально для сайта, являются визуальными материалами интерфейса и не должны представляться как документальные фотографии фактической подачи блюда в ресторане. Реальные фотографии ресторана и конкретной подачи желательно использовать при наличии оригинальных материалов от Saigon VN.
