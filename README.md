# Saigon VN — new website

Новый коммерческий сайт ресторана Saigon VN в Королёве.

## Концепция

Тёмная версия выбранного дизайн-направления: чистая структура, тёплые оттенки дерева и света, золотые/красные акценты и быстрый доступ к главным действиям — меню, звонок и маршрут.

## Технологии

- semantic HTML5
- responsive CSS без UI-фреймворков
- минимальный vanilla JavaScript
- CSS-анимация пара над главным блюдом без canvas/WebGL
- Restaurant schema.org markup
- mobile sticky action bar
- responsive breakpoints для 375 / 430 / 768 / 1024 / 1440 px
- отдельная 404-страница и robots.txt

Сборщик, npm-зависимости и backend для запуска сайта не нужны.

## Установка локального репозитория

Понадобится Git. Откройте Terminal / PowerShell и выполните:

```bash
git clone https://github.com/Benzilya/saigon-new.git
cd saigon-new
```

Если репозиторий уже клонирован, повторно клонировать его не нужно.

## Запуск локально

Рекомендуется запускать сайт через простой HTTP-сервер, а не двойным кликом по `index.html`: так поведение будет ближе к реальному хостингу.

### Вариант 1 — Python

Если установлен Python 3:

```bash
python -m http.server 8080
```

На Windows также может использоваться:

```bash
py -m http.server 8080
```

После запуска открыть:

```text
http://localhost:8080
```

### Как выбрать другой порт

Число в конце команды — это порт. Например:

```bash
python -m http.server 3000
python -m http.server 5500
python -m http.server 9000
```

Тогда адреса будут соответственно `http://localhost:3000`, `http://localhost:5500` или `http://localhost:9000`.

Если терминал пишет, что порт занят (`Address already in use`), выберите другой, например 8081 или 9000.

Чтобы остановить локальный сервер, нажмите `Ctrl + C` в терминале.

### Вариант 2 — Node.js

Если установлен Node.js, можно без установки зависимостей проекта выполнить:

```bash
npx serve . -l 8080
```

И открыть `http://localhost:8080`.

### Вариант 3 — VS Code

Можно установить расширение Live Server, открыть папку проекта и нажать `Go Live`. Порт отображается в адресной строке браузера и обычно равен 5500.

## Как получить последние обновления

Перед началом работы перейдите в папку проекта и выполните:

```bash
git switch main
git pull origin main
```

Проверить состояние локальной копии:

```bash
git status
```

Если у вас есть локальные изменения, Git может не дать выполнить `pull`, пока они не будут сохранены коммитом или временно убраны через `git stash`.

Быстрый вариант с `stash`:

```bash
git stash
git pull origin main
git stash pop
```

После `stash pop` нужно проверить, не возникли ли конфликты.

## Как сохранить свои изменения

```bash
git status
git add .
git commit -m "Describe changes"
git push origin main
```

Для серьёзных изменений безопаснее создавать отдельную ветку и Pull Request:

```bash
git switch -c feature/my-change
git add .
git commit -m "Describe changes"
git push -u origin feature/my-change
```

## Структура проекта

```text
saigon-new/
├── index.html      # основной контент, SEO и структура страницы
├── styles.css      # вся визуальная система и responsive layout
├── steam.css       # анимация реалистичного пара
├── app.js          # мобильное меню и небольшие UI-поведения
├── 404.html        # страница 404
├── robots.txt      # правила для поисковых роботов
└── README.md       # документация
```

## Публикация

Сайт статический, поэтому его можно публиковать на GitHub Pages, Netlify, Cloudflare Pages, Vercel или обычном веб-хостинге. Build command не требуется; публикуется корень репозитория.

Для GitHub Pages: `Settings` → `Pages` → в `Build and deployment` выбрать `Deploy from a branch` → ветка `main` → папка `/ (root)` → `Save`.

После включения Pages изменения из `main` обычно публикуются автоматически после нового push/commit.

## Что менять чаще всего

Контент страницы находится в `index.html`. Основные цвета и responsive-настройки — в `styles.css`. Эффект пара изолирован в `steam.css`, поэтому его можно настраивать или отключать независимо от остального сайта.

## Источники фактических данных

Контакты, адрес, рейтинг и отзывы взяты с текущего сайта Saigon VN / карточки Яндекс. Неизвестные часы работы и цены намеренно не выдуманы. Кнопка «Меню и цены» ведёт в текущий каталог ресторана, пока структурированное меню не будет перенесено в репозиторий.

Фото блюд и интерьерный референс в текущей версии — фотографии Unsplash. Кнопка «Посмотреть реальные фото» ведёт в галерею Saigon VN на Яндекс Картах. При получении оригинальных фотографий их можно заменить без изменения структуры страницы.
